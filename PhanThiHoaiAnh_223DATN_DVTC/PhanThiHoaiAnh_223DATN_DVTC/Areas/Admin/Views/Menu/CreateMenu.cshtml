@model MenuModel
@{
    ViewData["title"] = "Tạo mới thực đơn";
}
<h4>Tạo thực đơn</h4>
<div class="col-md-9">
    <form asp-action="CreateMenu" enctype="multipart/form-data">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <div class="form-group">
            <label>Tên menu</label>
            <input asp-for="Name" class="form-control" />
            <span asp-validation-for="Name" class="text-danger" />
        </div>
        <div class="form-group">
            <label>Chọn món</label>
            @foreach (var foodItem in Model.FoodItems)
            {
                <div>
                    <input type="checkbox" name="selectedFoodItems" value="@foodItem.Id" />
                    <label>@foodItem.Name</label>
                </div>
            }
        </div>
       
      
        <div class="form-group">
            <label>Hình ảnh</label>
            <input asp-for="ImageUpload" class="form-control" />
            <span asp-validation-for="ImageUpload" class="text-danger" />
            <img id="imgpreview" class="pt-2" />
        </div>

        <div class="form-group">
            <label>Mô tả</label>
            <textarea asp-for="Description" class="form-control"></textarea>
            <span asp-validation-for="Description" class="text-danger" />
        </div>

        <div class="form-group">
            <label>Trạng thái</label>
            <select asp-for="Status" class="form-control">
                <option>--Chọn trạng thái</option>
                <option value="true">Đang kinh doanh</option>
                <option value="false">Ngừng kinh doanh</option>
            </select> <span asp-validation-for="Status" class="text-danger">
            </span>
        </div>

        <div class="form-group">
            <button class="btn btn-primary">Tạo thực đơn</button>
        </div>

    </form>
    <a asp-action="Index">Trở về</a>
</div>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script src="https://cdn.ckeditor.com/4.22.1/standard/ckeditor.js"></script>
    <script>
        CKEDITOR.replace('Description');
    </script>

}